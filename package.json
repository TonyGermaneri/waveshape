{
  "name": "waveshape",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Reference-grade real-time audio waveform / spectral analyzer built on WebGPU compute shaders.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test 'src/**/*.test.ts'"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
